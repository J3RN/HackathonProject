%h1.centered Dashboard

%h2 Recent artwork
%div.dashboard-images
  - Image.all.each do |image|
    - if (image.id != current_user.id)
      = link_to image_tag(image.image.url), image_path(image)

- unless current_user.feed.empty?
  %h3 Recent artwork from people you've followed
  %div.dashboard-images
    - current_user.feed.each do |u|
      = link_to image_tag(u.image.url), image_path(u)
