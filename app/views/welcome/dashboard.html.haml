%h1 Welcome to your dashboard, #{current_user.name}!
= link_to "New Image", 'images/new', :class => "btn btn-blue"
= link_to "New Album", 'albums/new', :class => "btn btn-blue"
%h2 Recent artwork
- Image.all.each do |image|
	= link_to image_tag(image.image.url), images_path(image)
%h3 Recent artwork from people you've followed
- current_user.feed.each do |u|
	= link_to image_tag(u.image.url), images_path(u)